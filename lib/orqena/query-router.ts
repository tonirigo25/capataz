export type QueryIntent = "clients" | "works" | "budgets" | "invoices" | "suppliers" | "agenda" | "tasks" | "documents" | "treasury" | "completeness";
export function queryIntent(text: string): QueryIntent | null { const value = text.toLocaleLowerCase("es"); for (const [intent, words] of Object.entries({ clients: ["cliente"], works: ["trabajo", "obra", "proyecto"], budgets: ["presupuesto"], invoices: ["factura"], suppliers: ["proveedor"], agenda: ["agenda", "cita"], tasks: ["tarea"], documents: ["documento"], treasury: ["tesorería", "cobro", "pago", "saldo"], completeness: ["falta", "completo", "cuáles"] }) as Array<[QueryIntent, string[]]>) if (words.some((word) => value.includes(word))) return intent; return null; }
