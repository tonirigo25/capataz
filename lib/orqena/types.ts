export type OrqenaEntityType = "company" | "client" | "work" | "budget" | "invoice" | "supplier" | "task" | "document";
export type SourceLink = { entityType: OrqenaEntityType; entityId: string; label: string; href: string; observedAt: string; reliability: "record" | "confirmed-memory" | "document" };
export type ConversationState = { lastIntent?: string; lastEntityType?: OrqenaEntityType; lastEntityId?: string; lastClientId?: string; lastWorkId?: string; lastBudgetId?: string; lastInvoiceId?: string; lastSupplierId?: string; lastMissingFields?: string[]; lastProposal?: unknown; activeTask?: unknown; pendingConfirmation?: PendingConfirmation; unresolvedQuestion?: string; lastReferencedEntities?: Array<{ type: OrqenaEntityType; id: string }>; contextUpdatedAt: string };
export type PendingConfirmation = { id: string; companyId: string; conversationId: string; action: string; entityType: OrqenaEntityType; payload: Record<string, unknown>; review: Record<string, unknown>; createdAt: string; expiresAt: string };
export type StructuredResponse = { text: string; presentation?: "entity-summary" | "missing-fields" | "proposal" | "confirmation" | "agenda" | "treasury" | "success" | "error-recovery"; data?: unknown; sources?: SourceLink[] };
