# Latest continuous review

Status: `SUCCESS` — the persistent review service is healthy and publicly
reachable.

- Stable URL: `https://orqena-review-web-review.up.railway.app`
- Deployed SHA: `766bbfeb17d399883113e83a959ce73326689d31`
- Successful deployment ID: `86b72560-b4fb-40c3-a0b7-638ccf320260`
- Updated: 2026-07-26 21:44 CEST
- Staging changed: no
- Production changed: no

## Remote evidence

- The sole pre-deploy migration owner ran `npm run db:deploy`; all 43 tracked
  migrations are applied and no migration is pending.
- `/api/health/live`, `/api/health/ready` and `/api/status` return HTTP `200`.
- Twelve public pages/endpoints return HTTP `200`.
- The public Playwright matrix passed 32 cases and explicitly skipped 28
  inapplicable cases across Chromium, Firefox and WebKit, 320–1920 px, reduced
  motion, axe, image, overflow and noindex checks.
- Two identical synthetic demo submissions produced one lead, one audit event
  and one email outbox entry. No live email was sent. A cross-site submission
  returned `403` and persisted zero records.
- The persistent review database contains two synthetic companies and eleven
  role-specific QA profiles. A new one-time PLATFORM_OWNER reset URL is
  generated for each audit run and delivered separately; no password, cookie or
  reset token is committed.
- The first authenticated remote matrix is running separately from development
  work. Its evidence is not yet represented as a V5 pass.

## Visible changes in this SHA

- C0 release identity, provenance, compatibility and closure ledgers.
- Persistent consented demo funnel with rate limiting, deduplication, audit,
  retention and outbox-only email.
- Seven-stage public product story, sector routes, ROI hypothesis tool,
  first-week onboarding and mobile capability copy.
- Trust, status, security, disclosure and search/resource surfaces.
- Exact Lighthouse, low-end, PWA, Playwright, dependency and secret gates.
- Railway standalone startup now binds `0.0.0.0`; the npm 10 clean-install
  lockfile is reproducible.

## Recommended routes

- `/`
- `/demo`
- `/producto`
- `/sectores`
- `/planes`
- `/seguridad`
- `/estado`
- `/contacto`
- `/recursos/calculadora-roi`
- `/recursos/diagnostico-operativo`
- `/login`
- `/hoy` after using the separately delivered one-time synthetic access

## Known incidents and open gates

- Baseline deployment `ad630afc-5c66-4616-95c3-eb591b06a287` failed because
  Railway's container hostname was inherited instead of binding `0.0.0.0`.
  Deployment `86b72560-b4fb-40c3-a0b7-638ccf320260` proves the repair.
- Candidate `1f434dabf08079680f069ea9b522ef597ccc601d` failed during `npm ci`
  because the lockfile generated by npm 11 omitted npm 10 transitive entries.
  The deployed SHA proves a clean npm 10 install.
- Local Lighthouse records home LCP `2623 ms`, 123 ms above the C3 target of
  `2500 ms`; C3 remains `IN_PROGRESS`.
- Authenticated route/profile/state coverage, remote capacity recovery,
  representative-data migration rehearsal, backup/restore, full staging,
  production go/no-go and human approval are not yet PASS.
- Real-user comprehension, physical Safari/iOS and Chrome/Android devices,
  NVDA/VoiceOver, signed mobile artifacts, legal/brand signatures and real
  provider activation remain `READY_FOR_EXTERNAL_INPUT`.
- Public indexing, live billing, live email, live fiscal transmission and all
  other live providers remain disabled.
