// Dry-run is the default. This wrapper never enables deletion by itself.
// A future authorized execution must pass --execute and the exact approval
// environment value required by audit-production-fixtures.mjs.
import "./audit-production-fixtures.mjs";
