import { acceptInvitation } from "./actions";
export default async function AcceptInvitationPage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token=""}=await searchParams;return <main className="screen mx-auto max-w-lg py-12"><div className="card p-6"><p className="type-label">Invitación</p><h1 className="type-page-title mt-2">Únete al equipo en Orqena</h1><p className="type-secondary mt-3">La invitación se comprobará con tu cuenta y solo puede utilizarse una vez.</p><form action={acceptInvitation} className="mt-6"><input type="hidden" name="token" value={token}/><button className="primary-button w-full">Aceptar invitación</button></form></div></main>}
