import { getEntitlements, requireCapability } from "@/lib/commercial/authorization";
import { planCatalog } from "@/lib/commercial/plans";
import { prisma } from "@/lib/prisma";
import { randomUUID } from "node:crypto";
import { changeLocalPlan, openStripeCustomerPortal, startStripeCheckout } from "./actions";

export default async function PlanUsagePage() {
  const auth = await requireCapability("company.billing.manage");
  const commercial = await getEntitlements(auth.companyId);
  const [members, usage, priceMappings, billingCustomer] = await Promise.all([
    prisma.companyMembership.count({ where: { companyId: auth.companyId, status: "active" } }),
    prisma.usageRecord.groupBy({ by: ["metric"], where: { companyId: auth.companyId }, _sum: { quantity: true } }),
    auth.role === "OWNER" && process.env.BILLING_ENABLED === "true" ? prisma.billingPriceMapping.findMany({ where: { provider: "stripe", active: true }, orderBy: [{ planKey: "asc" }, { interval: "asc" }] }) : Promise.resolve([]),
    auth.role === "OWNER" ? prisma.billingCustomer.findUnique({ where: { companyId: auth.companyId } }) : Promise.resolve(null)
  ]);
  return <main className="screen"><h1 className="type-page-title">Plan y uso</h1><div className="mt-6 grid gap-4 lg:grid-cols-3"><section className="card p-5 lg:col-span-2"><p className="type-label">Plan actual</p><h2 className="mt-2 text-2xl font-bold">{planCatalog[commercial.planKey as keyof typeof planCatalog]?.name ?? commercial.planKey}</h2><p className="type-secondary mt-2">Estado: {commercial.subscription?.status ?? "Plan base compatible"}</p>{commercial.subscription?.trialEndsAt ? <p className="mt-2 text-sm">Prueba hasta {commercial.subscription.trialEndsAt.toLocaleDateString("es-ES")}</p> : null}</section><section className="card p-5"><p className="type-label">Miembros</p><p className="mt-2 text-3xl font-bold">{members} <span className="text-base font-normal text-content-secondary">/ {String(commercial.values.max_members ?? "—")}</span></p></section></div><section className="card mt-4 p-5"><h2 className="type-section-title">Consumo</h2><div className="mt-3 grid gap-2 sm:grid-cols-2">{usage.length ? usage.map((item) => <div key={item.metric} className="rounded-lg bg-subtle p-3"><strong>{item.metric}</strong><p className="type-secondary">{String(item._sum.quantity ?? 0)}</p></div>) : <p className="type-secondary">Todavía no hay consumo medido en este periodo.</p>}</div></section>{auth.role === "OWNER" && priceMappings.length ? <section className="card mt-4 p-5"><h2 className="type-section-title">Suscripción y facturas</h2><p className="type-secondary mt-2">Stripe solo se abre después de tu confirmación. Orqena no aplica cargos por sobreuso.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><form action={startStripeCheckout} className="grid gap-3"><select name="planKey" className="field">{[...new Set(priceMappings.map((item) => item.planKey))].map((key) => <option key={key} value={key}>{planCatalog[key as keyof typeof planCatalog]?.name ?? key}</option>)}</select><select name="interval" className="field"><option value="month">Mensual</option><option value="year">Anual</option></select><input type="hidden" name="confirm" value="CONTINUAR_STRIPE"/><input type="hidden" name="idempotencyKey" value={`checkout:${auth.companyId}:${randomUUID()}`}/><button className="primary-button">Continuar a pago seguro</button></form>{billingCustomer ? <form action={openStripeCustomerPortal} className="grid content-start gap-3"><p className="type-secondary">Gestiona método de pago, facturas y cancelación en el portal del proveedor.</p><input type="hidden" name="confirm" value="ABRIR_PORTAL"/><input type="hidden" name="idempotencyKey" value={`portal:${auth.companyId}:${randomUUID()}`}/><button className="secondary-button">Abrir portal de cliente</button></form> : null}</div></section> : <section className="card mt-4 p-5"><h2 className="type-section-title">Simular cambio de plan</h2><p className="type-secondary mt-2">Proveedor local: no cobra, no usa claves y registra el cambio en auditoría.</p><form action={changeLocalPlan} className="mt-4 flex flex-col gap-3 sm:flex-row"><select name="planKey" className="field">{Object.entries(planCatalog).map(([key, plan]) => <option key={key} value={key}>{plan.name}</option>)}</select><input type="hidden" name="confirm" value="CAMBIAR"/><button className="primary-button">Aplicar cambio local</button></form></section>}</main>;
}
