import { requireCompanyContext } from "@/lib/auth/session";import { getEntitlements } from "@/lib/commercial/authorization";import { planCatalog } from "@/lib/commercial/plans";import { prisma } from "@/lib/prisma";import { changeLocalPlan } from "./actions";
export default async function PlanUsagePage(){const auth=await requireCompanyContext();const commercial=await getEntitlements(auth.companyId);const [members,usage]=await Promise.all([prisma.companyMembership.count({where:{companyId:auth.companyId,status:"active"}}),prisma.usageRecord.groupBy({by:["metric"],where:{companyId:auth.companyId},_sum:{quantity:true}})]);return <main className="screen"><h1 className="type-page-title">Plan y uso</h1><div className="mt-6 grid gap-4 lg:grid-cols-3"><section className="card p-5 lg:col-span-2"><p className="type-label">Plan actual</p><h2 className="mt-2 text-2xl font-bold">{planCatalog[commercial.planKey as keyof typeof planCatalog]?.name??commercial.planKey}</h2><p className="type-secondary mt-2">Estado: {commercial.subscription?.status??"Plan base compatible"}</p>{commercial.subscription?.trialEndsAt?<p className="mt-2 text-sm">Prueba hasta {commercial.subscription.trialEndsAt.toLocaleDateString("es-ES")}</p>:null}</section><section className="card p-5"><p className="type-label">Miembros</p><p className="mt-2 text-3xl font-bold">{members} <span className="text-base font-normal text-content-secondary">/ {String(commercial.values.max_members??"—")}</span></p></section></div><section className="card mt-4 p-5"><h2 className="type-section-title">Consumo</h2><div className="mt-3 grid gap-2 sm:grid-cols-2">{usage.length?usage.map(item=><div key={item.metric} className="rounded-lg bg-subtle p-3"><strong>{item.metric}</strong><p className="type-secondary">{String(item._sum.quantity??0)}</p></div>):<p className="type-secondary">Todavía no hay consumo medido en este periodo.</p>}</div></section>{auth.role==="OWNER"?<section className="card mt-4 p-5"><h2 className="type-section-title">Simular cambio de plan</h2><p className="type-secondary mt-2">Proveedor local: no cobra, no usa claves y registra el cambio en auditoría.</p><form action={changeLocalPlan} className="mt-4 flex flex-col gap-3 sm:flex-row"><select name="planKey" className="field">{Object.entries(planCatalog).map(([key,p])=><option key={key} value={key}>{p.name}</option>)}</select><input type="hidden" name="confirm" value="CAMBIAR"/><button className="primary-button">Aplicar cambio local</button></form></section>:null}</main>}
