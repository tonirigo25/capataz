import{prisma}from"@/lib/prisma";import{requirePlatformAccount}from"@/lib/commercial/platform";import{createSupportGrant,closeSupportGrant,toggleCompanySuspension}from"./actions";
export default async function PlatformPage(){const actor=await requirePlatformAccount();const [companies,grants]=await Promise.all([prisma.company.findMany({include:{subscriptions:{include:{plan:true},take:1,orderBy:{createdAt:"desc"}},_count:{select:{memberships:true,usageRecords:true}}},orderBy:{createdAt:"desc"},take:100}),prisma.supportAccessGrant.findMany({where:{platformAccountId:actor.platformAccountId,status:"ACTIVE"},include:{company:true}})]);return <main className="screen"><p className="type-label">Plataforma interna · {actor.platformRole}</p><h1 className="type-page-title mt-2">Administración de tenants</h1><div className="mt-6 grid gap-3">{companies.map(c=><article className="card grid gap-3 p-4 lg:grid-cols-[1fr_auto]" key={c.id}><div><strong>{c.nombreComercial}</strong><p className="type-secondary">{c.commercialStatus} · {c.subscriptions[0]?.plan.name??"Plan base"} · {c._count.memberships} membresías</p><p className="mt-1 text-xs text-content-tertiary">La plataforma no concede acceso a los datos del tenant.</p></div><div className="flex flex-wrap gap-2"><form action={createSupportGrant} className="flex flex-wrap gap-2"><input type="hidden" name="companyId" value={c.id}/><input required name="reason" className="field h-10 max-w-48" placeholder="Motivo obligatorio"/><input name="ticket" className="field h-10 max-w-32" placeholder="Ticket"/><input type="hidden" name="minutes" value="30"/><button className="secondary-button">Soporte 30 min</button></form>{actor.platformRole!=="PLATFORM_ANALYST"?<form action={toggleCompanySuspension}><input type="hidden" name="companyId" value={c.id}/><input type="hidden" name="suspend" value={String(c.commercialStatus!=="SUSPENDED")}/><input type="hidden" name="reason" value="Cambio confirmado desde plataforma interna"/><button className="ghost-button">{c.commercialStatus==="SUSPENDED"?"Reactivar":"Suspender"}</button></form>:null}</div></article>)}</div>{grants.length?<aside className="fixed inset-x-3 bottom-20 z-50 rounded-xl bg-amber-100 p-4 shadow-card lg:left-auto lg:right-5 lg:w-96"><strong>Acceso temporal activo</strong>{grants.map(g=><form action={closeSupportGrant} key={g.id} className="mt-2 flex items-center justify-between gap-2"><span className="text-sm">{g.company.nombreComercial} · hasta {g.expiresAt.toLocaleTimeString("es-ES")}</span><input type="hidden" name="grantId" value={g.id}/><button className="ghost-button">Finalizar</button></form>)}</aside>:null}</main>}
