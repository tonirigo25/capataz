import { saveBusinessOnboarding } from "./actions";
import { requireCompanyContext } from "@/lib/auth/session";
import { prisma } from "@/lib/prisma";
import { sectorProfiles } from "@/lib/business-profile/sectors";
import { resolveBusinessProfile } from "@/lib/business-profile/resolve-profile";
import { brand } from "@/lib/brand";
export default async function OnboardingPage() { const auth = await requireCompanyContext(); const company = await prisma.company.findUniqueOrThrow({ where: { id: auth.companyId } }); const selected = resolveBusinessProfile(company); return <main className="screen max-w-3xl"><header className="mb-6"><p className="type-label">Configura tu espacio</p><h1 className="type-page-title mt-2">Adapta {brand.productName} a tu negocio</h1><p className="type-secondary mt-2">Puedes guardar y continuar en otro momento. No se crearán datos de ejemplo.</p></header><form action={saveBusinessOnboarding} className="grid gap-5"><section className="card p-4"><h2 className="type-section-title">1. Tu actividad</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label><span className="label">Tipo de organización</span><select className="field mt-1" name="organizationType" defaultValue={company.organizationType ?? "COMPANY"}><option value="SELF_EMPLOYED">Autónomo</option><option value="COMPANY">Empresa</option></select></label><label><span className="label">Sector</span><select className="field mt-1" name="sectorKey" defaultValue={company.sectorKey ?? selected.key}>{Object.values(sectorProfiles).map((item) => <option key={item.key} value={item.key}>{item.name}</option>)}</select></label><label className="sm:col-span-2"><span className="label">Nombre visible</span><input className="field mt-1" name="displayName" defaultValue={company.nombreComercial}/></label></div></section><section className="card p-4"><h2 className="type-section-title">2. Tu forma de trabajar</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label><span className="label">Cómo llamas a un trabajo</span><input className="field mt-1" name="workSingular" defaultValue={selected.terminology.workSingular}/></label><label><span className="label">En plural</span><input className="field mt-1" name="workPlural" defaultValue={selected.terminology.workPlural}/></label><label className="sm:col-span-2"><span className="label">Objetivo principal</span><input className="field mt-1" name="mainGoal" placeholder="Ej. controlar cobros y organizar proyectos"/></label><label className="sm:col-span-2"><span className="label">Primera acción</span><select className="field mt-1" name="firstAction"><option>Crear un cliente</option><option>Preparar un presupuesto</option><option>Organizar mi agenda</option><option>Preguntar a Orqena</option></select></label></div></section><input type="hidden" name="step" value="7"/><div className="flex flex-col gap-2 sm:flex-row sm:justify-end"><button className="secondary-button" name="complete" value="false">Guardar y continuar después</button><button className="primary-button" name="complete" value="true">Terminar configuración</button></div></form></main>; }
