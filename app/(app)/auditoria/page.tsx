import{requireCapability}from"@/lib/commercial/authorization";import{prisma}from"@/lib/prisma";
export default async function AuditPage(){const auth=await requireCapability("company.view");const logs=await prisma.auditLog.findMany({where:{companyId:auth.companyId},orderBy:{createdAt:"desc"},take:100});return <main className="screen"><h1 className="type-page-title">Auditoría</h1><p className="type-secondary mt-2">Acciones administrativas y sensibles sin secretos ni contenido de documentos.</p><div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[42rem] text-left text-sm"><thead><tr className="border-b border-border"><th className="p-3">Fecha</th><th className="p-3">Acción</th><th className="p-3">Destino</th><th className="p-3">Motivo</th></tr></thead><tbody>{logs.map(log=><tr key={log.id} className="border-b border-border"><td className="p-3">{log.createdAt.toLocaleString("es-ES")}</td><td className="p-3 font-medium">{log.action}</td><td className="p-3">{log.targetType}</td><td className="p-3">{log.reason??"—"}</td></tr>)}</tbody></table></div></main>}
